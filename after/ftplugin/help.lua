-- although I prefer opening documentation in a vertical split this does not
-- work as well on different size monitors and swaywm scaling so I am disabling
-- it for now

-- local autocmd = vim.api.nvim_create_autocmd

-- -- stylua: ignore
-- autocmd({ "BufEnter" }, {
--   pattern = "*.txt",
--   command = "wincmd L",
-- })
